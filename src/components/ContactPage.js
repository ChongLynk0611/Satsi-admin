import Contact from "./Contact/Contact";

const ContactPage = () => {
    setTimeout(function(){
        window.scrollTo(0,0);
    }, 100)
    return (  
        <Contact />
    );
}
 
export default ContactPage;